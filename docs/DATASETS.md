# Datasets

## RICO (UI dataset)

Para descargar el dataset RICO, usa el script:

```bash
RICO_URL=URL_DEL_DATASET ./scripts/rico_download.sh
```

Opcional:

```bash
RICO_URL=URL_DEL_DATASET RICO_SHA256=HASH ./scripts/rico_download.sh
```

El script guarda el archivo en `datasets/rico/`.
