/home/lucy-ubuntu/Lucy_Workspace/Proyecto-VSCode/logs/*.log {
    daily
    missingok
    rotate 7
    compress
    delaycompress
    notifempty
    create 640 lucy-ubuntu lucy-ubuntu
    sharedscripts
    postrotate
        # reload systemd service if needed, e.g.
        # systemctl --user reload lucy-core.service >/dev/null 2>&1 || true
        echo "Rotated Lucy logs"
    endscript
}
